namespace Exo.Settings.Ui.ViewModels;

internal sealed class EmbeddedMonitorFeaturesViewModel
{
	private readonly DeviceViewModel _device;

	public EmbeddedMonitorFeaturesViewModel(DeviceViewModel device) => _device = device;
}
